# Math Ninja — заметки разработчика

## Активные системы
- `game.js` теперь содержит основную игровую логику (режимы, движение врагов, крафт, босс).
- `styles.css` отвечает за визуал, вынесен из `index.html`.
- Крафт работает через два слота и четырёхзначный набор операций. Перенос шурикенов — кликом по слоту и затем по числу.

## Недавние правки
- Исправлен критичный баг с кликом по шурикену: раньше бросок вызывал `ReferenceError` из-за отсутствия `drawShuriken`, теперь есть собственный неоновый рендерер сюрикена.
- Полностью переработан визуал: неоновый фон, стеклянный UI, обновлённые полосы прогресса и пауз-скрин рисуются в `drawBackground`, `drawHud` и `drawPauseSheet`.
- Стартовый HTML теперь подключает стили из `styles.css`, вся inline-верстка очищена.
- Первые волны идут строго по вертикали. Горизонтальная динамика плавно включается через `createBehavior`, чтобы игрок успевал освоиться.
- В `maybeGrantRetry` снято ограничение на количество «вторых шансов» — если ответ исчез, новый сюрикен вернётся вне зависимости от прошлых промахов.
- Облегчил визуал: уменьшены blur/box-shadow/анимации в CSS и орбов в `initBackground()`, чтобы фон стал легче для CPU, сохранив неоновую стилистику.

## Режимы игры
- **Бесконечный**: бесконечные волны базовых и элитных врагов. Цель — держать счёт как можно дольше.
- **Сюжетный**: 15 стандартных врагов + босс с тремя щитами. Каждый щит — отдельный пример.

## Босс
- Босс создаётся через `spawnBossTarget()`. В `answers` хранятся ответы для щитов, в `exprs` — текущие выражения.
- Тело босса живёт до тех пор, пока не разрушены все щиты. Снятие щита уменьшает радиус и подменяет выражение.

## Движение врагов
- Поведение задаётся объектом `behavior` (`sine`, `zigzag`, `drift`, `hover`) и обновляется в `updateTargets`.
- Скорость падения плавно растёт через счётчик спавнов `spawnCounter` и easing.

## Крафт
- Кулдаун общий (`CRAFT_COOLDOWN_MS`). Функция `resolveCraft` допускает деление с округлением до двух знаков.
- В `assignToCraftSlot` и `returnCraftSlot` больше нет автоперевода на другой слот: после синтеза нужно заново нажать на нужную ячейку.

## Дополнительные числа
- `addRandomShuriken` с вероятностью 18% выдаёт числа 1–20 для крафта, помимо правильных значений и балансных выпадений.

## TODO / идеи
- Добавить короткую анимацию разрушения щита босса.
- Расширить подсказки крафта всплывающими уведомлениями, чтобы игрок видел результат синтеза до начала кулдауна.
- Поддержать сохранение рекорда бесконечного режима в `localStorage`.
